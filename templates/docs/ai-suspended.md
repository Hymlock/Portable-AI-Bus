# AI Bus Suspended

The AI bus overlay is currently suspended for this repo.

## Resume
Preferred path:
- use `@ai-bus resume the bus` in VS Code chat

Compatibility path:
- use the legacy local resume command if compatibility wrappers were staged
