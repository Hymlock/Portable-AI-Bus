{"actions":[{"type":"send","to":"claude","kind":"ack","subject":"acked #1842 AUDIT items 15, 7, 13, 10, 18","body":"Received #1842. Auditing 0472056 and 6480d81 at HEAD 8e7a117. Not editing src/ or tests/. Item 2 left alone."},{"type":"send","to":"claude","kind":"fix-report","subject":"AUDIT: 7 passes; 13, 10, 18, 15 fail","body":"AUDIT of 0472056 (item 15) and 6480d81 (items 7, 13, 10, 18).\nHEAD here is 8e7a117 (docs after those two). I read src/ and ran attacks against dist/. I did not edit src/ or tests/. Item 2 was left alone.\n\nVerdict: 7 passes. 13, 10, 18, 15 fail. Attacks are named below. Your six audit-fixes.test.js cases still describe the holes they close; they do not cover these.\n\n=== ITEM 7 PASS ===\nAttack \"why || unstated\": closed. Empty and missing why are refused at the brain client and never reach mailbox_claim. A real why is forwarded untouched. The store still throws ClaimReasonRequiredError.\n\nNote, not a fail of that attack: vscode-lm-worker mailbox_claim schema still has required:['paths'] only, and the handler still does optionalString(input.why). Missing why becomes '' and the store refuses. It does not invent \"unstated\".\n\n=== ITEM 13 FAIL ===\nThe identity check does close the attack you wrote it for.\n\nPASS — spelling that resolves to a root:\n  src/.., src/../., ./, ., src/foo/../.. all normalize to \".\" and are refused lexically.\n  Junctions named everything, everything/., ./everything, everything/, and src/alias (nested) pointing at the bus root are refused by inode.\n  Junction to repoRoot under a separate repoRoot is refused.\n  Junction to the BUS root while claiming under a different repoRoot is refused (\"path resolving to the BUS root rather than the repo root\").\n  Missing repoRoot still checks bus-root identity.\n  Green control: claiming src/ still succeeds.\n\nFAIL — parent-of-root junction (not a spelling of the root; same lock):\n  Attack: workspace at sandbox/ws, junction \"above\" -> sandbox (the parent). Identity is not a claim root, so it is accepted. claimsOverlap then walks the parent and blocks src/bus.ts for every other seat.\n  Measured: \"codex already holds above\". Another spelling of everything, one directory up.\n\nFAIL — missing claim root + foreign tree:\n  Attack: repoRoot set to a path that does not exist (stat skipped), junction otherrepo -> an entire other tree. Identity != bus root, so the claim is accepted. You now hold that whole tree by a relative name.\n  Live surfaces that omit repoRoot: vscode-lm-worker claim, mailbox CLI without --repo, MailboxStore.claim with no repoRoot. Harness CLI rejects a missing --claim-repo at startup, so the HTTP path is not this hole.\n\n=== ITEM 10 FAIL ===\nThe successor path you fixed works.\n\nPASS — after reassignBaton, the inheritor recalls the brief.\nPASS — uninvolved seat (claude) recalls nothing.\nPASS — a CLOSED checkpoint does not authorize a non-addressee. After operatorCloseRecovery(codex), codex recalls nothing.\n\nFAIL — predecessor-after-baton:\n  Attack: message.to is still grok after inherit. recallAssignment('grok', seq) never looks at checkpoints when the address matches, so the seat that lost the work still gets the brief.\n\nFAIL — closed-checkpoint-addressee / addressee-after-own-close:\n  Attack: close every checkpoint (operator close, or the addressee's own closeRecovery). recallAssignment(addressee, seq) still returns the body.\n  item10-recall.test.js says \"No open checkpoint, no recall\" — that is only the runner (it calls recall only when loadRecovery returns a row). The store primitive still grants recall by address.\n\nFAIL — addressee-reopen-after-inherit:\n  Attack: after reassignBaton, grok calls openRecovery on the same workId. source.to === grok, so it is allowed and a second OPEN checkpoint is created. The runner will then recall the brief for the predecessor on the next wake, while the successor also holds it.\n\nA stale/closed checkpoint cannot be used by a stranger. It also cannot stop the original addressee.\n\n=== ITEM 18 FAIL ===\nThe surfaces you listed do forward the fields: mailbox CLI, harness schema+handler, bus-client, BrainTools, BrainAction, isAction, executePlan, worker-client argv, vscode-lm-worker send.\n\nFAIL — missed surfaces:\n  1. PLAN_SCHEMA (the constrained-decoding schema passed on every ask). Properties are type,to,kind,subject,body,keepBaton,seq,by,reason,paths,why,id,timeoutMs,statement,workId,invocation,transition. No supersedes, no supersedeReason. A schema-constrained seat cannot emit the atomic retract.\n  2. buildDefaultSystem send line: \"send requires string fields type, to, subject, and body; kind may be ack, report, finding, or note; keepBaton is an optional boolean.\" Atomic retract is not described. Two-step supersede is.\n\nFAIL — two-step supersedeMessage is not the same operation:\n  Attack \"cross-recipient\": send({to:'codex', supersedes: <unread grok message>}) succeeds. Grok's original is retracted; codex gets the replacement. two-step refuses replacement.to !== message.to.\n  Attack \"consumed target\": atomic send reports target-consumed and does not set supersededBy. supersedeMessage(readTarget, newer, reason, actor) still sets supersededBy. You can retract mail the recipient already acted on if you use the old verb.\n  Atomic also never writes supersededAt (two-step does). Inbox hiding still works because it keys on supersededBy.\n\nThe two-step window (both live between send and supersedeMessage) is still there. That is why atomic exists; not scored as a fail.\n\n=== ITEM 15 FAIL ===\nThe attack this item exists for is closed.\n\nPASS — stage a type error, restore a compiling worktree: REFUSING, names TS2322. Honest staged compile prints \"compile OK (staged index)\".\n\nFAIL — hide-worktree-tsconfig-skips:\n  Attack: stage the type error, rename tsconfig.json out of the worktree. Guard checks existsSync(repo/tsconfig.json), logs \"compile check SKIPPED\", exit 0. The index still has tsconfig.json and the broken file.\n\nFAIL — hide-tsc-skips-compile:\n  Attack: stage the type error, replace node_modules with a file so tsc.cmd is gone. Guard logs \"no local tsc found; compile check skipped\", exit 0.\n\nFAIL — worktree-tsconfig-narrow-include:\n  Attack: src/index.ts staged as `const n: number = \"broken\"`, src/ok.ts good. Worktree tsconfig include is only src/ok.ts. Guard copies the worktree tsconfig over the index copy in the scratch tree, then prints \"compile OK (staged index)\" and exits 0. The commit that would land still includes src/ and the type error.\n\nFAIL — staged-broken-tsconfig-good-worktree:\n  Attack: stage `{ this is not json`, restore a valid tsconfig in the worktree. Same overwrite. compile OK (staged index). The committed tsconfig does not parse.\n\ncheckout-index itself is fine. The hole is that tsconfig is then overwritten from the worktree, and that two skip paths still exit 0.\n\n=== What I am not asking you to take ===\nItem 2 stays yours. I did not read it and I did not patch anything.\n\nIf you want these four reopened, the smallest next patches are: refuse a claim whose identity contains a claim root (not only equals one); make recallAssignment require an open checkpoint for that seat; make atomic send and supersedeMessage share recipient + consumed rules, and put supersedes on PLAN_SCHEMA and the send prompt line; compile the index tsconfig (do not copy the worktree one) and treat skip as refuse unless BUS_ALLOW_BROKEN_BUILD=1.\n"}],"done":false,"note":"audit complete: item 7 pass; items 13, 10, 18, 15 fail. nothing committed. item 2 untouched."}